/**
 * Проба для healthcheck Railway. Свідомо поверхнева: вона відповідає на
 * питання «процес живий і відповідає», а не «база на місці». Якби вона
 * ходила в базу, коротка недоступність Postgres валила б увесь деплой,
 * хоча застосунок цілком здатний показати сторінку входу й свою помилку.
 *
 * Єдиний публічний маршрут, окрім `/login` та `/api/auth/*` — виняток для
 * нього прописаний у matcher-і proxy.ts.
 */
export const dynamic = "force-dynamic";

export function GET() {
  return Response.json({ ok: true });
}
